# 实验12 · 金融Agent构建及优化 —— 操作手册

本手册说明如何在本机安装、配置、运行 Financial Agent 系统，如何使用四大功能模块，以及如何运行各类实验并生成报告。

---

## 目录

1. [环境准备](#1-环境准备)
2. [配置 API Key](#2-配置-api-key)
3. [快速启动 Web 界面](#3-快速启动-web-界面)
4. [四大功能模块操作指南](#4-四大功能模块操作指南)
5. [示例数据与测试 PDF](#5-示例数据与测试-pdf)
6. [运行实验](#6-运行实验)
7. [查看结果与生成报告](#7-查看结果与生成报告)
8. [项目结构说明](#8-项目结构说明)
9. [常见问题与排错](#9-常见问题与排错)

---

## 1. 环境准备

### 1.1 软件要求

| 软件 | 版本要求 |
|---|---|
| Python | 3.9+（本项目在 3.11.9 上开发验证） |
| 操作系统 | Windows / macOS / Linux |
| 网络 | 需要能够访问 DeepSeek API 与数据集下载源 |

### 1.2 安装依赖

进入项目根目录后执行：

```bash
pip install -r requirements.txt
```

主要依赖：`openai`（调用 DeepSeek）、`streamlit`（Web 界面）、`pandas`/`numpy`/`matplotlib`/`scikit-learn`/`scipy`（数据分析与建模）、`pdfplumber`（PDF 解析）、`jieba`（中文分词）、`reportlab`（生成示例 PDF）、`python-pptx`（生成 PPT）。

> 提示：如果你在下载数据集时遇到 HuggingFace 超时，可设置镜像后运行：
> ```bash
> set HF_ENDPOINT=https://hf-mirror.com
> ```

---

## 2. 配置 API Key

系统默认使用 **DeepSeek** 大模型（OpenAI 兼容接口）。

1. 在项目根目录找到 `.env` 文件（如果不存在则新建一个）。
2. 填入你的 API Key 与模型配置：

```ini
# .env
DEEPSEEK_API_KEY=sk-你的密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
```

3. 可选参数（不填则使用默认值）：

```ini
# 主实验公平性参数（MoCA-Agent 与 Financial Agent 保持一致）
DEFAULT_TEMPERATURE=0.0
DEFAULT_MAX_TOKENS=4096
```

> ⚠️ 安全提醒：`.env` 中的 Key 是敏感信息，`.gitignore` 已将其排除，**请勿**把 `.env` 提交到公开仓库或在公开渠道分享。若 Key 意外泄露，请在 DeepSeek 后台重置。

---

## 3. 快速启动 Web 界面

在项目根目录执行：

```bash
streamlit run app.py
```

浏览器会自动打开 `http://localhost:8501`，即可看到系统主页。

### 3.1 侧边栏设置

| 设置项 | 说明 |
|---|---|
| DeepSeek API Key | 默认读取 `.env`，也可在此临时修改 |
| 模型 | `deepseek-chat`（推荐，快且省 Token）/ `deepseek-reasoner` |
| Temperature | 控制输出随机性，实验建议保持 0.0 |
| 最大输出 Token | 生成代码/长文时建议 4096+ |
| 任务专用 Prompt | 开关（默认开）；关闭即"w/o Task-specific Prompt"消融 |
| 允许执行 Python 代码 | 开关（默认开）；关闭即"w/o Code Execution"消融 |
| 查看 Token 用量统计 | 显示当前会话累计调用次数与 Token |

### 3.2 功能模块导航

在左侧 **功能模块** 单选框中切换四个模块。

---

## 4. 四大功能模块操作指南

### 4.1 Coding Agent

**用途**：根据自然语言任务生成、解释、修改并执行 Python 代码，运行失败时自动修复重试（最多 3 次）。

操作步骤：
1. 在"任务描述"输入框填写任务，例如：
   - *"读取 data/sample_data/示例股票日收盘价.csv，计算日收益率的标准差（日波动率）并输出。"*
   - *"解释下面这段代码"*（配合"已有代码"输入框）
   - *"修复这段代码中的错误"*（配合"已有代码"输入框）
2. （可选）上传 CSV / Excel 数据文件，或粘贴"已有代码"。
3. 点击 **运行**。

输出内容：任务类型识别结果、生成/修复后的代码、执行日志、计算结果、图表（如有）。

### 4.2 金融文档问答

**用途**：上传金融 PDF（年报/财务报告/公告），系统自动完成 解析 → 切分 → 索引 → 检索 → 证据定位 → LLM 回答，并标注来源页码。

操作步骤：
1. 上传 PDF 文件；或勾选"使用内置示例年报"直接使用 `data/pdfs/示例财务报告.pdf`。
2. 在"问题"输入框输入问题，例如：
   - *"公司2023年的营业收入是多少元？"*
   - *"公司2023年的毛利率是多少？"*
   - *"2023年净利润同比增长多少？"*
3. 点击 **回答**。

输出内容：回答正文、检索到的 Top-K 证据（含页码、类型、相似度）、原始证据片段。

> 说明：若证据不足，系统会明确回答"证据不足"，不会编造数字。

### 4.3 金融数据分析

**用途**：读取 CSV / Excel 金融数据，自动完成金融指标计算、数据处理、统计分析、可视化与结果解释。

操作步骤：
1. 上传 CSV / Excel 数据文件，或勾选"使用内置示例财务数据"。
2. 在"分析目标"输入框填写目标，例如：
   - *"计算2022和2023年的营业收入增长率、净利润增长率、毛利率，并绘制折线图，分析趋势。"*
   - *"计算各年 ROA、ROE、资产负债率，并做相关性分析。"*
3. 点击 **开始分析**。

输出内容：生成的分析代码、执行日志、图表、以及结合金融背景的数值解释。

**命令行独立运行**：金融数据分析为独立可移植包 `data_analysis/`，可脱离界面单独运行：
```bash
python -m data_analysis.run_analysis \
  --data data/sample_data/示例公司财务数据.xlsx \
  --question "计算2022和2023年的营业收入增长率、毛利率并绘图"
```
更多用法见 `data_analysis/README.md`。

### 4.4 数学建模

**用途**：按"问题理解 → 变量与假设 → 模型选择 → 建模求解 → 代码执行 → 结果评价 → 可视化与结论"的完整流程完成建模任务。

操作步骤：
1. （可选）上传数据文件，或勾选"使用内置示例建模数据"（企业信用评价数据）。
2. 在"建模问题"输入框填写问题，例如：
   - *"基于企业信用评价示例数据.csv 中企业的财务指标，采用熵权法综合评价并排序，识别信用风险最高的企业。"*
   - *"基于投资组合日收益率.csv 中三只资产的日收益率，构建 Markowitz 最小方差组合，求权重与年化风险收益。"*
3. 点击 **开始建模**。

输出内容：建模设计（问题理解/变量假设/模型选择/求解方案）、生成的代码、执行日志与结果、模型评价与结论、可视化图表。

---

## 5. 示例数据与测试 PDF

运行一次即可生成全部示例数据：

```bash
python experiments/prepare_data.py
```

生成的文件：

| 文件 | 用途 |
|---|---|
| `data/sample_data/示例公司财务数据.xlsx` | 金融数据分析（三年利润表/资产负债表） |
| `data/sample_data/示例股票日收盘价.csv` | 收益率/波动率计算、收益预测 |
| `data/sample_data/投资组合日收益率.csv` | 投资组合优化建模 |
| `data/sample_data/企业信用评价示例数据.csv` | 信用风险评价 / 企业综合评价 |
| `data/pdfs/示例财务报告.pdf` | 金融文档问答 |
| `data/pdfs/示例分红公告.pdf` | 金融文档问答 |

---

## 6. 运行实验

### 6.1 一键运行全部实验

```bash
python run_all.py
```

依次执行：主对比实验 → 消融实验 → 扩展能力实验 → 定性案例分析。

可跳过部分实验：

```bash
python run_all.py --skip-main --skip-ablation
# 可选：--skip-main --skip-ablation --skip-extension --skip-cases
```

### 6.2 分步运行

| 实验 | 命令 | 说明 |
|---|---|---|
| 生成固定测试题集 | `python experiments/benchmark.py`（内部自动生成） | FinQA 20 + FinanceMath 20 |
| 主对比实验 | `python experiments/run_main.py` | MoCA-Agent vs Financial Agent |
| 消融实验 | `python experiments/run_ablation.py` | w/o Prompt / w/o Code / Full |
| 扩展能力实验 | `python experiments/run_extension.py` | Coding / PDF 问答 / 数学建模 |
| 定性案例分析 | `python experiments/run_cases.py` | 4 个案例 |

### 6.3 冒烟测试（小规模验证）

正式跑全量前，先用少量题目验证流程：

```bash
python experiments/run_main.py --smoke 3     # 仅运行前 3 题
python experiments/run_ablation.py --smoke 3
```

### 6.4 断点续跑

实验脚本默认支持**断点续跑**：运行中断后再次运行同一命令，会跳过已完成的题目（结果保存在 `results/*_predictions.json`）。

---

## 7. 查看结果与生成报告

### 7.1 实验结果文件

所有结果保存在 `results/` 目录：

| 文件 | 内容 |
|---|---|
| `main_summary.json` / `main_*_metrics.json` | 主对比实验指标 |
| `main_moca_predictions.json` / `main_fa_predictions.json` | 每题预测明细（含耗时与 Token） |
| `ablation_summary.json` / `ablation_*_metrics.json` | 消融实验指标 |
| `ext_coding.json` / `ext_pdfqa.json` / `ext_modeling.json` | 扩展实验明细 |
| `cases.md` / `cases.json` | 定性案例分析 |
| `figures/` | 数学建模生成的图表 |

### 7.2 生成实验报告

```bash
python report/build_report.py
```

生成 `report/实验报告.md`，包含：实验目的、系统设计与实现、实验环境、主对比结果表、消融结果表、扩展能力结果、定性案例分析、结论与附录。

### 7.3 生成汇报 PPT

```bash
python report/build_ppt.py
```

生成 `report/实验12-金融Agent构建及优化.pptx`（14 页）。

### 7.4 重新评估 PDF 问答

若想用改进后的数字解析器（支持亿元/万元/百分号换算）离线重评 PDF 问答结果，无需再次调用模型：

```bash
python experiments/run_extension.py --reeval-pdfqa
```

---

## 8. 项目结构说明

```
.
├── app.py                     # Web 界面入口（streamlit run app.py）
├── run_all.py                 # 一键运行全部实验
├── config.py                  # 全局配置
├── llm.py                     # DeepSeek LLM 封装（记录 Token）
├── executor.py / exec_runner.py  # 安全代码执行沙箱
├── financial_agent/           # Financial Agent 模块（Coding/文档问答/数学建模/数值问答）
│   ├── coding_agent.py        #   Coding Agent
│   ├── doc_qa.py              #   金融文档问答（RAG）
│   ├── math_modeling.py       #   数学建模
│   ├── benchmark_agent.py     #   金融数值问答（主实验用）
│   └── prompts.py             #   任务专用 / 通用 Prompt
├── data_analysis/             # 金融数据分析（独立可移植包）
│   ├── analyzer.py            #   核心类 FinancialDataAnalysis
│   ├── run_analysis.py        #   命令行入口
│   ├── config.py / llm.py     #   自带配置与 LLM 封装
│   ├── executor.py / exec_runner.py  # 自带代码执行沙箱
│   ├── prompts.py / extract.py
│   └── README.md              #   独立运行说明
├── moca_agent/moca_agent.py   # MoCA-Agent 复现
├── experiments/               # 实验脚本
│   ├── run_main.py            #   主对比实验
│   ├── run_ablation.py        #   消融实验
│   ├── run_extension.py       #   扩展能力实验
│   ├── run_cases.py           #   定性案例
│   ├── benchmark.py           #   统一 Benchmark 接口
│   ├── eval.py                #   统一评价
│   ├── finparse.py            #   金融数字解析
│   ├── prepare_data.py        #   生成示例数据与 PDF
│   └── questions/             #   固定测试题集
├── data/                      # 数据集与示例数据
├── results/                   # 实验结果
├── report/                    # 实验报告与 PPT 生成脚本
├── scripts/                   # 演示应用与截图脚本
└── README.md                  # 项目说明
```

---

## 9. 常见问题与排错

**Q1：`import openai` 报错 / API 调用失败**
- 检查 `.env` 中 `DEEPSEEK_API_KEY` 是否正确；
- 检查网络能否访问 `https://api.deepseek.com`；
- 查看代码执行结果中的 `error` 字段获取具体原因。

**Q2：代码执行提示超时**
- 沙箱默认超时 15 秒（`config.py` 中 `EXEC_TIMEOUT`）。对复杂建模可调大：
  ```python
  EXEC_TIMEOUT = 30
  ```
- 若代码本身进入死循环，超时后会被强制终止。

**Q3：PDF 中文检索不到证据**
- 确保已安装 `jieba`（`pip install jieba`）；
- 重新运行 `python experiments/prepare_data.py` 生成含中文字体的示例 PDF。

**Q4：实验运行中断后如何继续**
- 直接重新运行同一命令即可，脚本会自动跳过 `results/` 中已完成的题目（断点续跑）。

**Q5：如何知道每次调用的 Token 消耗**
- 界面侧边栏点击"查看 Token 用量统计"；
- 实验结果的 `*_predictions.json` 中每题记录 `tokens.prompt_tokens / completion_tokens / total_tokens`。

**Q6：实验很慢 / 费用较高**
- 先运行冒烟测试（`--smoke 3`）确认无误再全量；
- 使用 `deepseek-chat` 而非 `deepseek-reasoner`，可显著降低延迟与成本；
- MoCA-Agent 每题约需 6–8 次模型调用，Token 消耗约为 Financial Agent 的 6 倍，属正常现象。

**Q7：想更换其他大模型**
- 只要提供 OpenAI 兼容接口，在 `.env` 中修改 `DEEPSEEK_BASE_URL` 与 `DEEPSEEK_MODEL` 即可。
